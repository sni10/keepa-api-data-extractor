<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheResult="false"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <php>
        <!--
          Force APP_ENV to 'test' using <env> to ensure it overrides
          both $_SERVER and $_ENV (since variables_order=EGPCS populates $_ENV from OS).
        -->
        <env name="APP_ENV" value="test" force="true"/>

        <!-- KERNEL_CLASS остаётся обычной переменной окружения -->
        <env name="KERNEL_CLASS" value="App\Kernel"/>

        <!-- Прочие переменные окружения для тестов (исторические, но безвредные) -->
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- <env name="DB_CONNECTION" value="sqlite"/> -->
        <!-- <env name="DB_DATABASE" value=":memory:"/> -->
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
    <coverage>
        <report>
            <text outputFile="php://stdout"/>
            <html outputDirectory="var/coverage-report"/>
        </report>
    </coverage>
</phpunit>
